#include "main.h"

/**
 * _isupper - function that checks if character is uppercase
 * @c - Character to check
 *
 * Return: 0
 */

int _isupper(int c)
{
	if (isupper(c))
		return (0);
	else
		return (1);
}
