#include "main.h"

/**
 * _abs - Function returning absolute value of variable
 * @n: The characteer to print
 *
 * Return: On success 1.
 * On error, 0 is returned.
 */

int _abs(int n)
{
	_putchar(abs(n));
	return (1);
}

